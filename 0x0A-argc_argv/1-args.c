#include <stdio.h>
#include "main.h"

/**
 * main - program printing name of itself
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: Always: 0 (Success)
 */

int main(int argc_atribue_((unused)), char *argv[])

{
	printf("%s\n", *argv);
	return (0);
}
